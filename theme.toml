name = "researcher"
description = "A simple portfolio and blog theme for Zola."
license = "BlueOak-1.0.0"
homepage = "https://github.com/lukehsiao/zola-researcher"
# The minimum version of Zola required
min_version = "0.22.0"
# An optional live demo URL
demo = "https://zola-researcher.pages.dev"

# Any variable there can be overridden in the end user `config.toml`
# You don't need to prefix variables by the theme name but as this will
# be merged with user data, some kind of prefix or nesting is preferable
# Use snake_casing to be consistent with the rest of Zola
[extra]
# The name shown in the navbar, post bylines, and the footer.
author = "John Doe"

# Links to include in the navigation bar. An empty link points at the site
# root. Links are relative to the base_url, so something like "cv.pdf" works
# if that file exists in your site's static directory.
nav = [
  {name = "About", link = ""},
  {name = "Blog", link = "blog/"},
]

# Your Google Analytics ID
analytics = ""

# KaTeX math rendering; see README
katex_enable = false

# instant.page prefetching; see README
instantpage_enable = true

# Load Font Awesome from its CDN; see README
fontawesome = false

# Extra stylesheets, relative to the site root, loaded after the theme's own
# CSS. Useful for overriding fonts or colors without forking the theme.
# custom_css = ["css/custom.css"]

# The theme author info: you!
[author]
name = "Luke Hsiao"
homepage = "https://luke.hsiao.dev"

# If this is porting a theme from another static site engine, provide
# the info of the original author here
[original]
author =  "Ankit Sultana"
homepage = "http://ankitsultana.com/"
repo = "https://github.com/ankitsultana/researcher"
